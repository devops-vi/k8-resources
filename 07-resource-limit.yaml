kind: Pod
apiVersion: v1
metadata:
  name: labels
  labels:
    project: expense
    module: backend
    environment: dev
spec:
  containers:
    - name: nginx
      image: nginx
# Search in google on how to apply resources in kubernetes

#kubectl describe pod limits